from utils import *
from utils import calculate_the_operation_i_choose as calculate

#user_input_num = float(input("Enter a number to check if it's even: "))

#output_calc = calc_op(9129312, 12322, "divide")
#print("The result of the calculation is:", output_calc)

#print("Is", user_input_num, "even?", is_even(user_input_num))

calculate(sum, 10, 5)
print(calculate(multiply, 4, 7))
